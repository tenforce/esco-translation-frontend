{{#if (not-eq controller.currentRouteName "sign-in")}}
  {{show-notifications}}
  {{nav-bar activateItem="activateItem"}}
{{/if}}

<div class="main-content">
	{{outlet}}
</div>

{{ember-notify closeAfter=8000}}