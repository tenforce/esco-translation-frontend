`import { notificationExpirationChecker } from '../../../helpers/notification-expiration-checker'`
`import { module, test } from 'qunit'`

module 'Unit | Helper | notification expiration checker'

# Replace this with your real tests.
test 'it works', (assert) ->
  result = notificationExpirationChecker 42
  assert.ok result
