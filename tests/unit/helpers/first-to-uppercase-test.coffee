`import { firstToUppercase } from '../../../helpers/first-to-uppercase'`
`import { module, test } from 'qunit'`

module 'Unit | Helper | first to uppercase'

# Replace this with your real tests.
test 'it works', (assert) ->
  result = firstToUppercase 42
  assert.ok result
