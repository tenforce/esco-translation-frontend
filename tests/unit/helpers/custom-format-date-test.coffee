`import { customFormatDate } from '../../../helpers/custom-format-date'`
`import { module, test } from 'qunit'`

module 'Unit | Helper | custom format date'

# Replace this with your real tests.
test 'it works', (assert) ->
  result = customFormatDate 42
  assert.ok result
